#pragma once
#include "graph.h"
#include "visualNode.h"

namespace nechto::ide
{
	/*class visualNodeFabric
	{
		std::shared_ptr<graph> nGraph;

		void operator()(nodePtr v1)
		{
			assert(v1.exist());
			std::set<nodePtr> connections;
			if(v1.exist())

		}
	};*/
}